# Dépendances RUNTIME de l'API /chat déployée en serverless sur Vercel.
# DOIT rester slim (< 500 Mo bundle) : miroir de [project].dependencies de pyproject.toml.
# Les outils lourds (Docling, Whisper, transformers, torch, Streamlit) sont en extras
# et ne doivent JAMAIS être installés sur Vercel.
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
pydantic-ai==1.36.0
openai>=1.58.0
asyncpg>=0.29.0
pgvector>=0.2.4
python-dotenv>=1.0.1
