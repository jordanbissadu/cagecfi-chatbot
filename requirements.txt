# =========================================
# Dépendances pour le déploiement Vercel (fonction serverless /chat).
# VOLONTAIREMENT MINIMAL : pas de docling / torch / whisper / streamlit
# (ceux-ci ne servent qu'à l'ingestion locale et feraient exploser la
# taille de la fonction serverless, limitée à 250 Mo).
#
# Pour le développement local complet, utiliser pyproject.toml :
#   uv pip install -e .
# =========================================

fastapi>=0.110.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
pydantic-ai==1.36.0
openai>=1.58.0
asyncpg>=0.29.0
pgvector>=0.2.4
python-dotenv>=1.0.1
